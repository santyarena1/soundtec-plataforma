---
description: En cada push al admin, cargar una novedad. El changelog no se edita en la UI.
alwaysApply: true
---

# Changelog automático (panel admin)

Las novedades del admin **no se cargan, editan ni borran en la UI**. La pantalla es solo lectura.

Van en `src/data/admin-changelog.ts` y el deploy las publica solo (`scripts/sync-changelog.ts` + sync al abrir el admin). Cualquier entrada que no esté en ese archivo se borra de la base.

Cada vez que se commitee/pushee un cambio visible para el equipo del admin:

1. Agregar **una entrada nueva** al array `SHIPPED_ADMIN_CHANGELOG`.
2. Usar un **id nuevo** (`ship-YYYY-MM-DD-tema`). No reutilizar un id viejo: si se reusa, quien ya tocó Entendido no vuelve a ver el popup.
3. Completar `version` (siguiente número), `releasedAt` (ISO), `summary` (frase de "En pocas palabras") e `items` con `kind`: `NUEVO` | `FIX` | `MEJORA`.
4. Textos en español, cortos, para el equipo interno. Nunca para el portal del cliente.

No agregar formularios, botones de editar/borrar ni acciones `upsert`/`delete` de changelog. Si el cambio no se ve en el admin, no hace falta entrada.
